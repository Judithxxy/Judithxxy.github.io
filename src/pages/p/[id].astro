---
import ProductPage from '../../components/ProductPage.astro';
import { buildProductPaths } from '../../lib/productPaths';

export async function getStaticPaths() {
  return buildProductPaths('products');
}
---

<ProductPage locale="en" {...Astro.props} />
